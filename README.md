## Install node modules

```` bash
> npm install

## Run in Dev Mode

```` bash
> npm run watch
> npm start # (in separate terminal)

## Build and Run

```` bash
> ./node_modules/.bin/webpack
> ./node_modules/.bin/electron .
